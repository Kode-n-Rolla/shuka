/*
    Greeting line with `shuka` naming
    flags:
        --explorer (eth, battlechain)
        --address (0x...)
        --chainid
        --out (-o) -> (no neccessary flag) save to certain dir
*/

use crate::error::ShukaError;

pub fn run() -> Result<(), ShukaError> {
    todo!()
}